# Build de producción (Vercel). NEXT_PUBLIC_* se inyecta en el bundle en build time.
# URL pública de la API del backend (VPS). No es un secreto: termina en el cliente igual.
NEXT_PUBLIC_API_URL=https://api.mirkocalzadilla.com/api/v1
